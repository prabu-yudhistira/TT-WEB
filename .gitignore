node_modules
.next
out
build
*.tsbuildinfo
next-env.d.ts

# local env & db
.env
.env.*
!.env.example
*.db
*.db-journal

# payload uploads (owner content, not source)
/media

.DS_Store
.superpowers/

# Verification harness outputs — screenshots and contact sheets
shots/
eyeshots/
# Screenshots from the samsara-* gates. Kept OUT of eyeshots/ on purpose:
# eyes-legibility.mjs readdir()s that directory and asserts it holds exactly the
# 14 expression crops, so anything else written there fails an unrelated gate.
samsarashots/
# eyes-zoom.mjs writes this into the repo root.
zoom-neutral.png
killswitch-*.png
occlusion-sheet.png
